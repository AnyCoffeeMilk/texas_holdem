import React from 'react'
import ThemeBtn from '../../_components/ThemeBtn'

export default function RaiseBtn({ onClick }) {
    return (
        <ThemeBtn onClick={onClick}>
            RAISE
        </ThemeBtn>
    )
}
